[pytest]
# Put the repository root on sys.path so the src/ package is importable as
# `from src.main import app` no matter which directory pytest is invoked from.
pythonpath = .
testpaths = tests
addopts = -ra
